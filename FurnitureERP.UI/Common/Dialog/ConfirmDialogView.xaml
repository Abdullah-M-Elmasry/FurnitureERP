<UserControl x:Class="FurnitureERP.UI.Common.Dialog.ConfirmDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        Width="400"
        Padding="20"
        CornerRadius="12"
        Background="White">

        <StackPanel>

            <TextBlock
                Text="{Binding Message}"
                FontSize="16"
                Margin="0,0,0,20"
                TextWrapping="Wrap"/>

            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Right">

                <Button
                    Content="Cancel"
                    Command="{Binding CancelCommand}"/>

                <Button
                    Content="Delete"
                    Margin="10,0,0,0"
                    Command="{Binding ConfirmCommand}"/>

            </StackPanel>

        </StackPanel>

    </Border>

</UserControl>