<UserControl x:Class="FurnitureERP.UI.Modules.Customers.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FurnitureERP.UI.Common.Controls">

    <controls:ERPListPage
        Title="Customers"
        Subtitle="Manage customers and contact information">

        <!-- TOOLBAR -->
        <controls:ERPListPage.ToolbarContent>

            <controls:ERPToolbar>

                <controls:ERPToolbar.LeftContent>

                    <StackPanel Orientation="Horizontal">

                        <Button Content="Add Customer"
                                Style="{StaticResource ERPPrimaryButton}"
                                Margin="0,0,8,0"
                                Command="{Binding OpenAddCustomerCommand}" />

                        <Button Content="Refresh"
                                Style="{StaticResource ERPSecondaryButton}"
                                Command="{Binding RefreshCommand}" />

                    </StackPanel>

                </controls:ERPToolbar.LeftContent>

                <controls:ERPToolbar.CenterContent>

                    <TextBox Width="250"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Search customer..."
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                </controls:ERPToolbar.CenterContent>

            </controls:ERPToolbar>

        </controls:ERPListPage.ToolbarContent>

        <!-- GRID -->
        <controls:ERPListPage.Content>

            <materialDesign:Card Padding="10"
                                  UniformCornerRadius="8">

                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="Name"
                                            Binding="{Binding Name}" />

                        <DataGridTextColumn Header="Phone"
                                            Binding="{Binding Phone}" />

                        <DataGridTextColumn Header="Email"
                                            Width="2*"
                                            Binding="{Binding Email}" />

                        <DataGridTextColumn Header="Address"
                                            Width="2*"
                                            Binding="{Binding Address}" />

                        <!-- ACTIONS -->
                        <DataGridTemplateColumn Header="Actions"
                                                Width="120">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <controls:ERPActionButtons
                                        Item="{Binding}"
                                        EditCommand="{Binding DataContext.EditCustomerCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"

                                        DeleteCommand="{Binding DataContext.DeleteCustomerCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}" />

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>

            </materialDesign:Card>

        </controls:ERPListPage.Content>

        <!-- PAGINATION -->
        <controls:ERPListPage.PaginationContent>

            <controls:ERPPagination
                CurrentPage="{Binding CurrentPage}"
                TotalPages="{Binding TotalPages}"
                PreviousCommand="{Binding PreviousPageCommand}"
                NextCommand="{Binding NextPageCommand}"
                FirstCommand="{Binding FirstPageCommand}"
                LastCommand="{Binding LastPageCommand}" />

        </controls:ERPListPage.PaginationContent>

    </controls:ERPListPage>

</UserControl>