<UserControl x:Class="FurnitureERP.UI.Modules.Inventory.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FurnitureERP.UI.Common.Controls">

    <controls:ERPListPage
        Title="Inventory"
        Subtitle="Manage inventory and stock movements">

        <!-- ================= TOOLBAR ================= -->

        <controls:ERPListPage.ToolbarContent>

            <controls:ERPToolbar>

                <controls:ERPToolbar.LeftContent>

                    <StackPanel Orientation="Horizontal">

                        <Button Content="Opening Balance"
                Margin="0,0,8,0"
                Style="{StaticResource ERPPrimaryButton}"
                Command="{Binding OpeningBalanceCommand}" />

                        <Button Content="Stock Adjustment"
                Margin="0,0,8,0"
                Style="{StaticResource ERPSecondaryButton}"
                Command="{Binding StockAdjustmentCommand}" />

                        <Button Content="Refresh"
                Style="{StaticResource ERPSecondaryButton}"
                Command="{Binding RefreshCommand}" />

                    </StackPanel>

                </controls:ERPToolbar.LeftContent>

                <controls:ERPToolbar.CenterContent>

                    <TextBox Width="250"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Search product..."
                             Text="{Binding SearchText,
                                            UpdateSourceTrigger=PropertyChanged}" />

                </controls:ERPToolbar.CenterContent>

            </controls:ERPToolbar>

        </controls:ERPListPage.ToolbarContent>

        <!-- ================= GRID ================= -->

        <controls:ERPListPage.Content>

            <materialDesign:Card
                Padding="10"
                UniformCornerRadius="8">

                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False">

                    <DataGrid.Columns>

                        <DataGridTextColumn
                            Header="Code"
                            Binding="{Binding Code}" />

                        <DataGridTextColumn
                            Header="Barcode"
                            Binding="{Binding Barcode}" />

                        <DataGridTextColumn
                            Header="Name"
                            Width="2*"
                            Binding="{Binding Name}" />

                        <DataGridTextColumn
                            Header="Category"
                            Binding="{Binding Category}" />

                        <DataGridTextColumn
                            Header="Unit"
                            Binding="{Binding Unit}" />

                        <DataGridTextColumn
                            Header="Stock"
                            Binding="{Binding CurrentStock}" />

                        <DataGridTextColumn
    Header="Last Updated"
    Width="170"
    Binding="{Binding UpdatedAt,
                      StringFormat={}{0:dd/MM/yyyy HH:mm}}" />

                        <!-- Status -->
                        <DataGridTemplateColumn Header="Status" Width="130">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <Border Padding="8,3"
                    CornerRadius="10">

                                        <Border.Style>

                                            <Style TargetType="Border">

                                                <Setter Property="Background"
                                Value="#4CAF50"/>

                                                <Style.Triggers>

                                                    <DataTrigger Binding="{Binding IsLowStock}"
                                         Value="True">

                                                        <Setter Property="Background"
                                        Value="#FF9800"/>

                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding IsOutOfStock}"
                                         Value="True">

                                                        <Setter Property="Background"
                                        Value="#F44336"/>

                                                    </DataTrigger>

                                                </Style.Triggers>

                                            </Style>

                                        </Border.Style>

                                        <TextBlock Text="{Binding Status}"
                           Foreground="White"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>

                                    </Border>

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                        <!-- Action -->

                        <DataGridTemplateColumn
                            Header="Actions"
                            Width="140">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <Button Content="Transactions"
                                            Style="{StaticResource ERPSecondaryButton}"
                                            Command="{Binding DataContext.OpenTransactionsCommand,
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>

            </materialDesign:Card>

        </controls:ERPListPage.Content>

        <!-- ================= PAGINATION ================= -->

        <controls:ERPListPage.PaginationContent>

            <controls:ERPPagination
                CurrentPage="{Binding CurrentPage}"
                TotalPages="{Binding TotalPages}"
                PreviousCommand="{Binding PreviousPageCommand}"
                NextCommand="{Binding NextPageCommand}"
                FirstCommand="{Binding FirstPageCommand}"
                LastCommand="{Binding LastPageCommand}" />

        </controls:ERPListPage.PaginationContent>

    </controls:ERPListPage>

</UserControl>